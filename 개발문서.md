# VEDA Multi-Sensor TCP Control System 개발문서

## 1. 프로젝트 개요

본 프로젝트는 TCP 통신을 이용하여 Ubuntu Linux 클라이언트 또는 웹 브라우저에서 Raspberry Pi 4에 연결된 장치를 원격 제어하는 리눅스 시스템 프로그래밍 실습 프로젝트이다.

평가 과제에서 요구하는 대상 장치는 LED, 부저, 조도센서, 7세그먼트이며, 각 장치 제어 기능은 공유 라이브러리(`.so`)로 분리하였다. 서버는 실행 중 `dlopen()`과 `dlsym()`으로 장치별 함수를 동적으로 로딩하므로 특정 장치 기능을 수정하거나 업그레이드할 때 해당 라이브러리만 재빌드 및 재배포할 수 있다.

## 2. 요구사항 대응

| 요구사항 | 구현 파일 | 구현 내용 |
|----------|-----------|-----------|
| TCP 원격 장치 제어 | `src/main.c`, `src/client.c` | 서버는 TCP 8000 포트에서 요청을 받고, CLI 클라이언트는 HTTP 형식의 GET 요청을 전송 |
| 서버: Raspberry Pi 4 | `src/main.c`, `lib/*.c` | wiringPi 기반 GPIO, PWM, I2C 제어 |
| 클라이언트: Ubuntu Linux | `src/client.c` | 호스트 PC용 `gcc`로 빌드되는 CLI 메뉴 제공 |
| 멀티 프로세스 또는 스레드 | `src/main.c` | `poll()`로 연결을 감시하고 요청별 detached pthread 생성 |
| 장치 공유 라이브러리 | `lib/led.c`, `lib/buzzor.c`, `lib/photoresistor.c`, `lib/segment.c` | `libled.so`, `libbuzzor.so`, `libphotoresistor.so`, `libsegment.so` 생성 |
| 클라이언트 시그널 처리 | `src/client.c` | `sigaction()` + `volatile sig_atomic_t` 플래그 방식으로 POSIX 비동기 안전성 준수. `SIGTSTP`/`SIGQUIT` 무시 |
| 서버 데몬 구성 | `src/main.c` | 기본 실행 시 이중 fork 데몬화, `-f`로 전경 실행 |
| 빌드 자동화 | `Makefile` | `make`, `make clean` 제공 |
| 활성 스레드 모니터링 | `src/main.c`, `resources/index.html` | 서버 내 `active_threads` 전역 배열과 뮤텍스로 활성 스레드를 추적하여 `/api/threads` JSON API로 제공, `index.html`에서 실시간 테이블 렌더링 |

## 3. 개발 일정

| 단계 | 기간 | 주요 작업 |
|------|------|-----------|
| 1단계 | 2026-06-01 | TCP 서버/클라이언트 구조 설계, 장치별 파일 분리 방향 수립 |
| 2단계 | 2026-06-02 | LED, 부저, 조도센서, 7세그먼트 기본 제어 구현 및 웹 리소스 연결 |
| 3단계 | 2026-06-03 | 동적 라이브러리 구조 확정, GPIO 공유 상태/뮤텍스 보강, 데몬 모드와 시그널 처리 보완 |
| 4단계 | 2026-06-04 | 클라이언트 입력 검증/시그널 안전성/연결 재시도 개선, 문서 정리 및 GitHub Issues 반영 |

## 4. 시스템 구조

```text
Ubuntu Linux CLI client        Web Browser
          |                        |
          +---------- TCP 8000 ----+
                                   |
                              src/main.c
                         poll + pthread server
                                   |
       +---------------------------+---------------------------+
       |                           |                           |
   libled.so                 libbuzzor.so              libsegment.so
       |                           |                           |
       +------------- lib/rpi_common.c + include/rpi_common.h +
                                   |
                         libphotoresistor.so
                                   |
                         Raspberry Pi 4 GPIO/I2C
```

서버는 정적 HTML 파일을 `resources/`에서 제공하며, 장치 제어 요청은 `/led`, `/buzz`, `/segment`, `/pr` 계열 API로 라우팅한다.

### 4.1 디렉토리 구조

```text
VEDA_4Sensor_TCP_Control/
├── Makefile                  # 크로스 컴파일 빌드 스크립트
├── README.md                 # 프로젝트 소개 및 빌드/실행 안내
├── 개발문서.md                # 본 문서
├── 실행_과정.md                # 빌드~시연 전체 절차
├── src/                      # 소스 코드
│   ├── main.c                # TCP 서버
│   └── client.c              # CLI 클라이언트
├── include/                  # 공유 헤더 파일
│   ├── rpi_common.h          # GPIO 초기화, 뮤텍스, 공유 상태
│   ├── led.h, buzzor.h       # 장치별 인터페이스
│   ├── photoresistor.h       #
│   └── segment.h             #
├── lib/                      # 장치 드라이버 소스 (→ .so)
│   ├── rpi_common.c          # wiringPi 초기화, GPIO 뮤텍스
│   ├── led.c, buzzor.c       # 장치별 구현
│   ├── photoresistor.c       #
│   └── segment.c             #
├── resources/                # 웹 대시보드 HTML
│   ├── index.html            # 메인 페이지
│   ├── led.html, buzzer.html # 장치별 UI
│   ├── segment.html          #
│   └── pr.html               #
└── docs/                     # 문서 및 이미지
    ├── images/               # 회로 구성도
    └── issue_report.md       # 이슈 해결 보고서
```

### 4.2 회로 구성도

아래 이미지는 Raspberry Pi 4, LED, 부저, PCF8591 ADC 조도센서, 1자리 7세그먼트 연결 구조를 설명하기 위한 회로 구성도이다.

![Raspberry Pi 4 Sensor and Output Circuit](./docs/images/raspberry-pi-4-sensor-output-circuit.png)

실제 제출 코드의 동작 기준은 `README.md`의 핀맵과 `include/rpi_common.h`, `lib/segment.c` 설정을 따른다. 특히 현재 7세그먼트 드라이버는 `COMMON_ANODE 1` 기준으로 동작하므로, 회로도 이미지의 세그먼트 타입 표기와 실제 코드 설정이 다를 경우 코드 설정을 우선한다.

## 5. 세부 구현 내용

### 5.1 서버

`src/main.c`는 TCP 서버의 중심 모듈이다.

| 항목 | 구현 |
|------|------|
| 포트 | `PORT 8000` |
| 연결 처리 | listening socket을 `poll()`로 감시 |
| 요청 처리 | 클라이언트 fd를 detached pthread에 넘겨 병렬 처리 |
| 동적 로딩 | `dlopen("./libled.so")`, `dlsym("led")` 방식 |
| 정적 파일 | `/`는 `resources/index.html`, 그 외 HTML은 `resources/<filename>`에서 제공 |
| 데몬화 | 기본 실행 시 이중 fork, `setsid()`, 표준 입출력 `/dev/null` 리다이렉션 |
| 디버그 실행 | `./server -f` 또는 `./server --foreground` |
| 시그널 처리 | `sigaction()`으로 `SIGINT`/`SIGTERM`/`SIGHUP` 등록, `volatile sig_atomic_t` 플래그로 안전 종료 |
| 잘못된 명령 응답 | 장치 라이브러리 반환값(`int`)  기반으로 `sendOk(200)` 또는 `sendError(400)` 분기 |
| 경로 순회 차단 | `sendData()`에서 `..` 경로 차단, 파일 열기 성공 후 200 OK 헤더 전송 |

데몬 실행 시 `/proc/self/exe`로 실제 실행 파일 경로를 찾고 해당 디렉토리로 이동한다. 이 때문에 `server`, `lib*.so`, `resources/`를 같은 배포 디렉토리에 두면 상대 경로로 라이브러리와 HTML을 찾을 수 있다.

### 5.2 클라이언트

`src/client.c`는 Ubuntu Linux에서 실행하는 CLI 클라이언트이다.

| 메뉴 | 기능 |
|------|------|
| 1 LED | ON, MID, OFF, 0~1024 밝기 값 입력. 잘못된 입력 시 에러 메시지 출력 |
| 2 Buzzer | `do`, `re`, `mi`, `fa`, `sol`, `la`, `si`, `do2`, `off` 입력 |
| 3 7-Segment | `start`, `off`, `0~9`(1자리), `show/0~9`(1자리) 입력. 잘못된 입력 시 에러 메시지 출력 후 메뉴 복귀 |
| 4 Photoresistor | 아날로그 조회, 디지털 조회, 자동 LED 제어 시작/중지, 메인 메뉴 복귀 |
| 5 Quit | 자동 제어 중지 요청 후 소켓 정리 및 종료 |

7세그먼트 입력 검증: 1자리 장치 특성에 맞게 `strlen == 1` 조건으로 `00` 같은 다중 자릿수 입력을 차단한다. 허용 입력은 `start`, `off`, `0~9`, `show/0~9`이며, 그 외 입력은 에러 메시지를 출력하고 메뉴로 복귀한다.

시그널 처리: `sigaction()`과 `volatile sig_atomic_t server_running` 플래그를 사용하여 POSIX 비동기 시그널 안전성을 준수한다. Ctrl+C 발생 시 시그널 핸들러는 플래그만 설정하고, 메인 스레드에서 안전하게 `pr/auto/stop` 요청 전송, 소켓 정리, 프로그램 종료를 수행한다. `SIGTSTP`, `SIGQUIT`은 무시하여 의도하지 않은 정지나 종료를 방지한다.

연결 재시도: `send_command()` 함수는 서버 연결 실패 시 최대 3회 시도(초기 1회 + 재시도 2회)를 지수 백오프(1초 → 2초) 방식으로 수행한다. 모든 시도가 실패하면 에러 메시지를 출력하고 메뉴로 복귀한다.

### 5.3 LED

`lib/led.c`는 LED 제어를 담당한다.

| 명령 | 동작 |
|------|------|
| `ON` | `softPwmWrite(LED_PIN, 100)` |
| `MID` | `softPwmWrite(LED_PIN, 50)` |
| `OFF` | `softPwmWrite(LED_PIN, 0)` 및 카운트다운 취소 플래그 설정 |
| `0~1024` | 입력 값을 softPwm 범위(0~100)로 매핑하여 밝기 제어 |

LED는 wiringPi 기준 1번 핀, BCM 18번 핀을 사용하며 `softPwmCreate()`로 초기화한 뒤 `softPwmWrite()`로 출력을 제어한다.

### 5.4 부저

`lib/buzzor.c`는 `softTone` 기반 부저 제어를 담당한다.

| 명령 | 동작 |
|------|------|
| `do`~`do2` | 계이름 주파수 재생 |
| `1`~`8` | 숫자 음계 재생 |
| 직접 주파수 | 입력 숫자를 Hz로 해석해 500ms 재생 |
| `OFF`, `0` | 부저 정지 |
| `fein`, `alert` | 알림 멜로디 재생 |

### 5.5 조도센서

`lib/photoresistor.c`는 PCF8591 I2C ADC와 디지털 조도 입력을 함께 지원한다.

| 함수 | 용도 |
|------|------|
| `get_cds_value()` | PCF8591 `0x48`에서 AIN0 아날로그 값을 읽음 |
| `get_pr_value()` | wiringPi 0번 핀의 디지털 값을 읽음 |
| `cdsControl()` | 독립 실행형 무한 루프 제어 함수. 서버에서는 종료성 문제 때문에 직접 사용하지 않음 |

현재 서버 자동 제어는 `get_pr_value()`를 1초마다 호출한다. 디지털 값이 `LOW(0)`이면 어두움으로 판단해 LED를 켜고, `HIGH(1)`이면 밝음으로 판단해 LED를 끈다.

### 5.6 7세그먼트

`lib/segment.c`는 Common Anode 7세그먼트를 제어한다.

| 명령 | 동작 |
|------|------|
| `start` | 9부터 0까지 1초 간격 카운트다운 |
| `0~9` | 지정 숫자부터 0까지 카운트다운 |
| `show/<0~9>` | 지정 숫자 단발 표시 |
| `OFF` | 표시 제거, LED OFF, 카운트다운 취소 |

카운트다운 중에는 `delay(1000)` 구간에서 GPIO 뮤텍스를 풀어 다른 LED/부저 요청이 오래 막히지 않도록 했다. 0에 도달하면 LED를 켜고 `libbuzzor.so`에서 `play_fein_style_alert`를 동적으로 찾아 알림음을 재생한다.

### 5.7 스레드 모니터링

서버는 동시 접속 요청을 비동기 스레드로 처리하는 멀티스레드 구조를 취하고 있다. 이 활성 스레드 상태를 실시간 모니터링하기 위해 서버 메모리 상에 스레드 관리 기법을 구현했다.

| 항목 | 내용 |
|------|------|
| 스레드 추적 | `ThreadInfo` 구조체 배열 및 Mutex (`thread_list_mutex`)로 실시간 등록/제거 |
| 정보 수집 | `getpeername()` 시스템 콜을 통해 클라이언트 IP 획득, 수행 중인 명령 및 스레드 시작 시각 기록 |
| 데이터 노출 | `GET /api/threads` 요청 시 활성 스레드 정보를 JSON 배열 형태로 응답 |
| 실시간 갱신 | 메인 웹 페이지(`index.html`)에서 1.5초 간격으로 Polling하여 대시보드에 렌더링 |

### 5.8 멀티스레드 아키텍처

서버는 비동기식으로 다수의 요청을 동시에 처리하고 센서 데이터를 연속 제어하기 위해 3가지 스레드를 설계하여 운용한다.

#### 1. 메인 스레드 (Main Thread)
* **역할**: 프로그램 실행 시 시작되어 소켓 초기화와 dynamic library 로드를 수행하고, `poll()` 루프를 돌며 클라이언트의 새로운 연결을 대기한다.
* **스레드 분기**: 연결 요청이 수립되면 파일 디스크립터를 동적으로 할당한 후 `pthread_create()`를 통해 `clnt_connection` 스레드를 분기하며, 동시성 오버헤드를 낮추고 종료 시 자원 회수를 보장하기 위해 `pthread_detach()` 처리한다.
* **안전 종료**: `SIGINT`/`SIGTERM` 등의 종료 시그널이 오면 `server_running` 플래그를 `0`으로 세팅해 `poll()` 루프를 탈출하고, 자동 조도 제어 스레드를 `join`한 뒤 `active_threads` 배열 상에 남은 활성 쓰레드가 모두 완료될 때까지 안전하게 대기(Graceful Shutdown)한 후에 라이브러리를 `dlclose()` 한다.

#### 2. 클라이언트 연결 스레드 (Client Connection Thread)
* **생성 및 수명**: 클라이언트 접속 시마다 동적으로 생성되며 **Detached** 모드로 구동되어 수행을 마치면 OS가 자원을 자동 회수한다.
* **스레드 정보 추적**: 스레드 시작 시 `thread_list_mutex`를 잠그고 전역 `active_threads` 배열에 자신의 `pthread_self()` 값, `getpeername()`으로 획득한 IP, 요청 URI(Command), 시작 시간 등의 메타데이터를 저장한다. 종료 시 다시 뮤텍스를 획득해 해당 항목의 활성 플래그(`is_activate`)를 `0`으로 수정한다.
* **실시간 노출 원리**: LED, 부저 등 순간 연산 장치 제어는 쓰레드 생성/처리 후 즉시 완료되어 수 밀리초 내에 사라지지만, 7세그먼트 카운트다운(`segment/start`)과 같이 동기적 루프(`delay(1000)`)를 수반하는 요청은 약 10초간 스레드가 유지되어 브라우저 상의 모니터링 대시보드에서 쉽게 실시간 상태로 관측할 수 있다.

#### 3. 자동 조도 제어 스레드 (Auto PR Control Thread)
* **생성 및 수명**: `/pr/auto/start` API 호출 시점에 백그라운드 구동을 위해 단 1개만 **Joinable** 스레드로 동적 생성된다.
* **제어 루프**: `auto_pr_running` 플래그가 `1`인 동안 1초 주기로 센서의 디지털 핀을 읽어 어두우면(`LOW`) LED ON, 밝으면(`HIGH`) LED OFF를 반복 제어하며 백그라운드에서 상시 수행된다.
* **제어 중지 및 소멸**: `/pr/auto/stop` 요청이 오거나 서버가 종료되면 `auto_pr_running` 플래그가 `0`으로 해제되며 스스로 제어 루프를 탈출해 정상 리턴하고, 호출(또는 메인) 스레드에서 `pthread_join(auto_pr_thread, NULL)`을 호출해 자원을 클린하게 정리한다.

## 6. 웹 대시보드

`resources/` 디렉토리는 브라우저용 HTML UI를 제공한다.

| 파일 | 기능 |
|------|------|
| `index.html` | 장치별 페이지 진입 |
| `led.html` | LED ON/MID/OFF 및 밝기 슬라이더 |
| `buzzer.html` | 계이름/알림음 부저 제어 |
| `segment.html` | 7세그먼트 단일 표시, 기본/커스텀 카운트다운, OFF |
| `pr.html` | 조도센서 아날로그/디지털 값 조회, 자동 LED 제어 |

## 7. 빌드 및 배포 구조

`Makefile`은 서버와 공유 라이브러리를 Raspberry Pi용 AArch64 바이너리로 빌드하고, CLI 클라이언트는 호스트용 `gcc`로 빌드한다.

| 결과물 | 대상 |
|--------|------|
| `server` | Raspberry Pi |
| `client` | Ubuntu Linux host |
| `libled.so` | Raspberry Pi |
| `libbuzzor.so` | Raspberry Pi |
| `libphotoresistor.so` | Raspberry Pi |
| `libsegment.so` | Raspberry Pi |
| `resources/` | Raspberry Pi |

배포 기준 경로는 `/home/sunbi/prj`이다.

## 8. 문제점 및 보완 사항

GitHub Issues 기준으로 프로젝트의 문제점을 추적했다. 개발 과정에서 발견된 주요 이슈와 해결 상태는 다음과 같다.

### 8.1 해결 완료

| 구분 | 내용 |
|------|------|
| 정적 리소스 경로 불일치 | HTML 파일 경로가 실행 환경과 달라 `index.html`을 찾지 못함 → 해결 |
| LED 핀 매핑 오류 | 실제 배선은 wPi 1/BCM 18인데 코드 정의가 다름 → 해결 |
| 동적 모듈 상태 불일치 | 모듈별 전역 상태가 `dlopen`/`dlclose` 사이에서 유지되지 않음 → 해결 |
| 7세그먼트 취소 레이스 | OFF 요청 후에도 카운트다운이 끝까지 진행됨 → 해결 |
| `/pr`와 `/pr.html` 라우팅 충돌 | 접두사 비교로 HTML 요청이 API로 오인됨 → 해결 |
| LED PWM 전환 후 digitalWrite 충돌 | PWM 핀에 `digitalWrite`를 사용해 LED 상태가 불안정 → 해결 |
| 데몬 경로 문제 | 데몬 전환 후 작업 디렉토리가 잘못되어 리소스를 찾지 못함 → 해결 |
| 부저 라이브러리 오타 | `gpio_unlck()` 오타로 런타임 오류 가능 → 해결 |
| 클라이언트 수신 안정성 | 단발 `recv()`로 응답 일부가 잘릴 수 있음 → 해결 |
| 클라이언트 입력 검증 | `atoi()`가 잘못된 문자열을 0으로 처리 → 해결 |
| 7세그먼트 입력 검증 | CLI에서 `start`, `off`, `0~9`, `show/0~9` 이외의 입력을 차단하지 않음 → 1자리 숫자 강제 검증으로 해결 |
| 시그널 처리 안전성 | `signal()` + 핸들러 내 I/O 호출 → `sigaction()` + `sig_atomic_t` 플래그 방식으로 해결 |
| 연결 실패 UX | 서버 접속 실패 시 즉시 반환 → 최대 3회 지수 백오프 재시도로 해결 |
| 클라이언트 Ctrl+C 클린업 누락 | Ctrl+C 종료 시 cleanup_client()를 호출해 서버에 pr/auto/stop 전송 및 소켓 해제를 수행하도록 단일화하여 해결 |
| 서버 종료 시 라이브러리 충돌 | 서버 종료 시 auto_pr_thread를 join하고, 활성 요청 스레드 종료를 대기한 후 dlclose()를 수행하여 충돌(Segmentation Fault) 방지 |
| 공유 변수 데이터 레이스 | shared_led_state, cancel_countdown_flag, auto_pr_running에 대한 세터/게터 및 읽기 시 mutex 보호 적용 |
| API 파라미터 입력 검증 누락 | atoi() 대신 strtol() 및 endptr 포인터 검증을 사용하여 잘못된 파라미터("abc" 등) 오동작 차단 |
| 스레드 모니터 버퍼 안정성 | 모니터 메타데이터 복사 시 널 문자 강제 종단 및 JSON 버퍼 크기 snprintf 잔여량 계산 방식을 통해 오버플로 방지 |
| 잘못된 명령에 200 OK 반환 | 장치 라이브러리 반환 타입을 `void` → `int`로 변경하여 실패 시 `400 Bad Request` 반환하도록 수정 |
| 정적 파일 경로 순회 취약점 | `sendData()`에서 `strstr(filepath, "..")`로 경로 순회 차단. 파일 열기 성공 후에 200 OK 헤더를 전송하도록 순서 변경 |

### 8.2 남은 보완 지점

| 구분 | 내용 |
|------|------|
| 조도센서 내부 루프 종료성 | `cdsControl()`이 무한 루프라 서버 스레드로 직접 사용하기 어려움. 현재는 `get_pr_value()` 주기 호출 방식으로 대체 |
| 카운트다운 시간 지터 | 뮤텍스 경합과 `delay(1000)` 기반 대기로 정확한 1초 간격이 밀릴 수 있음 |

## 9. 추가 기능

평가 기준의 추가 기능 항목에 대응하는 구현은 다음과 같다.

| 추가 기능 | 설명 |
|-----------|------|
| PWM 밝기 제어 | LED를 ON/OFF뿐 아니라 0~1024 범위로 제어 |
| 웹 대시보드 | CLI 외에 브라우저 기반 장치별 UI 제공 |
| FEIN 알림음 | 부저 단독 호출 및 7세그먼트 0 도달 시 알림 멜로디 재생 |
| 조도 기반 자동 LED 제어 | 서버 백그라운드 스레드가 조도 디지털 값을 읽어 LED 자동 ON/OFF |
| CLI 입력 검증 | LED 밝기 범위, 7세그먼트 명령어, 숫자 1자리 제한 등 클라이언트 측 유효성 검사 |
| 연결 재시도 백오프 | 서버 접속 실패 시 지수 백오프 방식으로 자동 재시도 |
| GitHub Issues 추적 | 문제 발견, 원인, 해결 내용을 이슈로 관리 |
| 실시간 활성 스레드 모니터 | 서버 내 동작 중인 비동기 스레드 상태(TID, IP, 명령어, Uptime)를 수집하여 웹 메인 대시보드에서 실시간 모니터링 기능 제공 |

## 10. 남은 개선 방향

| 항목 | 개선 방향 |
|------|-----------|
| 조도센서 루프 종료 | `cdsControl()` 내부에 종료 플래그와 start/stop 인터페이스 추가 |
| 카운트다운 정밀도 | POSIX timer 또는 monotonic clock 기반 시간 보정 도입 |
| 배포 편의성 | `deploy.sh` 또는 Makefile `deploy` 타겟 추가 |
| 라이선스 명확화 | MIT License 전문을 담은 `LICENSE` 파일 추가 |

## 11. 동적 라이브러리 업그레이드 고려사항

- **로딩 메커니즘**: 본 서버는 `dlopen()`을 사용하여 시작 단계에서 장치 라이브러리(`libled.so`, `libbuzzor.so` 등)를 최초 1회 메모리에 적재하여 사용합니다.
- **업그레이드 및 반영**: 동적 라이브러리 파일을 교체하더라도 실행 중인 서버의 메모리에는 기존 라이브러리 코드가 계속 상주하므로 변경사항이 즉시 반영되지 않습니다. 신규 라이브러리를 반영하려면 서버를 재시작해야 합니다. (무중단 리로드를 원할 경우 활성 스레드가 없는 안전한 시점에 `dlclose()` 후 새 `.so` 파일을 `dlopen()` 하는 리로드 API 구현이 가능합니다.)
